# 🔍 تشخیص دقیق مشکل اتصال موبایل

## گام‌های تست دقیق

### گام 1: تست HTTP ساده (بدون SSL)

**روی موبایل:**
```
به این آدرس بروید:
http://192.168.70.102/network-test.html
```

**نتایج احتمالی:**

#### ✅ اگر صفحه باز شد:
- **یعنی:** اتصال شبکه کار می‌کند
- **مشکل:** فقط HTTPS مشکل دارد (SSL certificate)
- **راه‌حل:** صفحه HTTPS را باز کنید و warning را accept کنید

#### ❌ اگر صفحه باز نشد:
- **خطای "Can't connect to server":**
  - موبایل به WiFi متصل نیست
  - IP لپتاپ تغییر کرده
  - Firewall مسدود می‌کند

- **خطای "Connection timeout":**
  - موبایل در شبکه دیگری است
  - Router traffic را block می‌کند
  - WiFi isolation فعال است

---

### گام 2: بررسی اتصال WiFi

**روی موبایل:**
1. Settings > WiFi
2. مطمئن شوید به همان WiFi متصل هستید
3. IP Address موبایل را یادداشت کنید

**روی لپتاپ:**
```bash
# بررسی کنید موبایل در لیست دستگاه‌های شبکه هست
ip neigh show | grep "192.168.70"
```

اگر IP موبایل را دیدید (مثلاً 192.168.70.194)، یعنی در همان شبکه است.

---

### گام 3: تست با IP موبایل

**روی لپتاپ، ping به IP موبایل:**
```bash
# جایگزین کنید با IP موبایل خودتان
ping -c 3 192.168.70.194
```

**نتیجه:**
- ✅ اگر جواب داد: اتصال دو طرفه کار می‌کند
- ❌ اگر جواب نداد: Router دارد traffic را block می‌کند (AP Isolation)

---

### گام 4: بررسی AP Isolation

برخی routerها قابلیت "AP Isolation" یا "Client Isolation" دارند که دستگاه‌ها را از هم جدا می‌کند.

**بررسی:**
1. وارد تنظیمات Router شوید (معمولاً 192.168.70.1)
2. به دنبال این تنظیمات بگردید:
   - AP Isolation
   - Client Isolation  
   - Wireless Isolation
   - Guest Network Isolation

3. اگر فعال بود، آن را غیرفعال کنید

---

### گام 5: تست HTTPS

**روی موبایل، بعد از اینکه HTTP کار کرد:**
```
به این آدرس بروید:
https://192.168.70.102
```

**SSL Warning:**
مرورگر می‌گوید "Your connection is not private" یا مشابه آن.

**iOS Safari:**
```
1. "Show Details" یا "Advanced" بزنید
2. "Visit this website" یا "Proceed to 192.168.70.102 (unsafe)" بزنید
3. ممکن است نیاز باشد دوباره تایید کنید
```

**Android Chrome:**
```
1. "Advanced" بزنید
2. "Proceed to 192.168.70.102 (unsafe)" بزنید
```

---

## 🔧 حل مشکلات خاص

### مشکل 1: AP Isolation فعال است

**علائم:**
- HTTP test page باز نمی‌شود
- Ping از لپتاپ به موبایل جواب نمی‌دهد
- Router logs می‌گوید "blocked"

**راه‌حل:**
- غیرفعال کردن AP Isolation در Router
- یا استفاده از Hotspot موبایل به جای WiFi router
- یا اتصال لپتاپ و موبایل با کابل USB (USB tethering)

### مشکل 2: SSL Certificate قبول نمی‌شود

**علائم:**
- HTTP کار می‌کند
- HTTPS باز نمی‌شود یا خطا می‌دهد

**راه‌حل برای iOS:**
```
Settings > General > About > Certificate Trust Settings
(اگر certificate را manually نصب کرده‌اید)
```

**راه‌حل آسان‌تر:**
از HTTP استفاده کنید برای development:
- به `http://192.168.70.102/network-test.html` بروید
- دکمه "تست HTTPS" را بزنید
- Warning را accept کنید

### مشکل 3: موبایل با 5GHz WiFi مشکل دارد

**علائم:**
- گاهی وصل می‌شود، گاهی نه
- Connection ناپایدار است

**راه‌حل:**
1. به Router login کنید
2. WiFi 2.4GHz را فعال کنید (علاوه بر 5GHz)
3. موبایل را به 2.4GHz وصل کنید

---

## 📊 دیباگ کامل

**اسکریپت تست شبکه:**
```bash
./troubleshoot-mobile.sh
```

این اسکریپت بررسی می‌کند:
- ✅ IP لپتاپ
- ✅ وضعیت serverها
- ✅ Nginx configuration
- ✅ اتصال localhost
- ✅ اتصال network IP

---

## 🎯 Checklist کامل

- [ ] لپتاپ به WiFi متصل است؟
- [ ] موبایل به همان WiFi متصل است؟
- [ ] IP لپتاپ: `192.168.70.102`
- [ ] Serverها در حال اجرا: `./troubleshoot-mobile.sh`
- [ ] HTTP test page باز می‌شود: `http://192.168.70.102/network-test.html`
- [ ] Ping از لپتاپ به موبایل کار می‌کند
- [ ] AP Isolation غیرفعال است
- [ ] SSL warning را accept کردید
- [ ] Cache مرورگر را پاک کردید
- [ ] Airplane mode toggle کردید

---

## 💡 نکته نهایی

اگر هیچ‌کدام کار نکرد، **از USB tethering استفاده کنید:**

1. موبایل را با USB به لپتاپ وصل کنید
2. روی موبایل: Settings > Personal Hotspot > USB
3. لپتاپ IP جدیدی می‌گیرد (مثلاً 172.20.10.2)
4. موبایل IP دارد (مثلاً 172.20.10.1)
5. از browser موبایل به `http://172.20.10.2:5173` بروید

---

**اول HTTP test page را امتحان کنید:**
```
http://192.168.70.102/network-test.html
```

اگر باز شد، بگویید تا مرحله بعد را راهنمایی کنم.
اگر باز نشد، بگویید دقیقاً چه خطایی می‌بینید.
