# API Base URL - Leave empty to use dynamic host (window.location.host)
# This allows the app to work on localhost, network IP, and mobile devices
# VITE_API_BASE_URL=

# Enable HTTPS in Vite dev server
HTTPS=true
