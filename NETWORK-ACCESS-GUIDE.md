# راهنمای اتصال از گوشی به سرور

## 🌐 اطلاعات شبکه

Your network IP is: `192.168.70.102`

**دسترسی:**
- 🌐 Frontend: `https://192.168.70.102`
- 🔧 Backend API: `https://192.168.70.102/api`
- 👨‍💼 Django Admin: `https://192.168.70.102/admin`

## 📱 نحوه اتصال از گوشی

### مرحله 1: اتصال به همان شبکه WiFi
- کامپیوتر و گوشی باید روی **همان شبکه WiFi** باشند
- اگر کامپیوتر کابل دارد، ممکن است روی شبکه متفاوتی باشد

### مرحله 2: باز کردن در مرورگر گوشی
```
https://192.168.70.102
```

### مرحله 3: قبول کردن SSL Certificate
1. مرورگر هشدار امنیتی نشان می‌دهد
2. روی **"Advanced"** یا **"پیشرفته"** کلیک کنید
3. روی **"Proceed"** یا **"ادامه دادن"** کلیک کنید

**⚠️ نکته مهم:** این هشدار طبیعی است چون از self-signed certificate استفاده می‌کنیم.

### مرحله 4: استفاده از سایت
- همه چیز باید عادی کار کند
- OTP ارسال می‌شود
- Login کار می‌کند
- QR Scanner دسترسی به دوربین دارد

## 🔍 عیب‌یابی

### مشکل 1: سایت باز نمی‌شود
**علت:** شبکه‌های مختلف

**راه‌حل:**
```bash
# در کامپیوتر، IP را دوباره چک کنید:
hostname -I

# IP جدید را در گوشی وارد کنید
```

### مشکل 2: "سایت در دسترس نیست"
**علت:** Firewall یا سرورها running نیستند

**راه‌حل در کامپیوتر:**
```bash
# چک کنید سرورها running هستند:
ps aux | grep "manage.py runserver"
ps aux | grep "vite"

# اگر نیستند، restart کنید:
./stop-https.sh
./start-https.sh &
```

### مشکل 3: SSL Certificate Accept نمی‌شود
**راه‌حل در Android:**
1. Chrome: Settings → Privacy → Clear browsing data
2. دوباره سایت را باز کنید
3. Advanced → Proceed

**راه‌حل در iOS (Safari):**
1. Settings → Safari → Clear History and Website Data
2. دوباره سایت را باز کنید
3. Show Details → visit this website

### مشکل 4: API ها کار نمی‌کنند
**چک کنید:**
```bash
# در کامپیوتر:
tail -f /tmp/django.log

# باید ببینید:
# [date] "POST /api/accounts/auth/send-otp/ HTTP/1.1" 200
```

### مشکل 5: Slow یا Timeout
**علت:** WiFi ضعیف

**راه‌حل:**
- به روتر نزدیک‌تر شوید
- یا از USB Tethering استفاده کنید (گوشی را به کامپیوتر وصل کنید و hotspot فعال کنید)

## 🎯 تست اتصال

### از کامپیوتر:
```bash
# تست Django:
curl -k https://192.168.70.102/api/accounts/users/ -I

# باید 200 برگردد
```

### از گوشی:
در مرورگر گوشی بروید به:
```
https://192.168.70.102/api/accounts/users/
```

باید یک JSON response ببینید.

## 📊 وضعیت سرورها

```bash
# چک کردن Django:
ss -tuln | grep 8000
# باید ببینید: 0.0.0.0:8000

# چک کردن Vite:
ss -tuln | grep 5173
# باید ببینید: 0.0.0.0:5173

# چک کردن Nginx:
ss -tuln | grep 443
# باید ببینید: 0.0.0.0:443
```

## 🔐 امنیت

**⚠️ هشدار:** 
- این تنظیمات فقط برای **Development** است
- در Production باید:
  - Certificate معتبر بگیرید
  - `DEBUG = False` کنید
  - `ALLOWED_HOSTS` را محدود کنید
  - Firewall را فعال کنید

## 🚀 دستورات سریع

### Restart همه چیز:
```bash
./stop-https.sh
sleep 2
./start-https.sh &
```

### چک کردن IP:
```bash
hostname -I
```

### چک کردن Logs:
```bash
# Django:
tail -f /tmp/django.log

# Vite:
tail -f /tmp/vite.log

# Nginx:
sudo tail -f /var/log/nginx/faydo-error.log
```

## 🌟 نکات مهم

1. **همیشه** HTTPS استفاده کنید (برای دوربین و GPS)
2. **همیشه** در همان شبکه باشید
3. **همیشه** SSL Certificate را accept کنید
4. اگر IP عوض شد، Nginx config را آپدیت کنید

## 📱 تست کامل از گوشی

1. ✅ به WiFi متصل شوید (همان WiFi کامپیوتر)
2. ✅ به `https://192.168.70.102` بروید
3. ✅ SSL warning را accept کنید
4. ✅ صفحه Home باید load شود
5. ✅ روی "ورود/ثبت نام" کلیک کنید
6. ✅ شماره تلفن را وارد کنید
7. ✅ کد OTP را دریافت کنید
8. ✅ وارد شوید
9. ✅ به داشبورد بروید
10. ✅ QR Scanner را تست کنید

## 🎉 اگر همه چیز کار کرد

تبریک! حالا می‌توانید:
- از گوشی خود سایت را تست کنید
- QR Code را scan کنید
- GPS و Location را تست کنید
- همه ویژگی‌های موبایل را چک کنید

---

**IP Address فعلی:** `192.168.70.102`  
**آخرین بروزرسانی:** 2025-11-03
